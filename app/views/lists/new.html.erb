<h2>投稿画面</h2>

<%= form_with model: List.new do |f| %>
    <h4>タイトル</h4>
    <%= f.text_field :title %>

    <h4>本文</h4>
    <%= f.text_area :body %>

    <h4>画像</h4>
    <%= f.file_field :image, accept: "image/*" %>
    <p></p>
    <%= f.submit "投稿" %>
<% end %>
